### connect to postgress commandline
`docker ps` (get container name)
`docker exec -it <container-name> bash
`psql -U root -d fishable`

# use nodejs 20+ (lts)
nvm use --lts
node --version
npm start

